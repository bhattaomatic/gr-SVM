This is the SVM-write-a-block package meant as a guide to building
out-of-tree packages. To use the SVM blocks, the Python namespaces
is in 'SVM', which is imported as:

    import SVM

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(SVM)
